<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Danh sách đặt chỗ</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="container">
    <h1>Danh sách Đặt Chỗ</h1>
    <a href="/bookings/new" class="btn">+ Thêm Đặt Chỗ Mới</a>
    <table class="styled-table">
      <thead>
        <tr>
          <th>ID</th>
          <th>Tên Khách Hàng</th>
          <th>Ngày</th>
          <th>Giờ</th>
          <th>Trạng Thái</th>
          <th>Hành Động</th>
        </tr>
      </thead>
      <tbody>
        <% if (bookings.length === 0) { %>
          <tr>
            <td colspan="6" class="no-data">Chưa có lịch đặt chỗ</td>
          </tr>
        <% } else { %>
          <% bookings.forEach((booking) => { %>
            <tr>
              <td><%= booking.displayId %></td>
              <td><%= booking.customerName %></td>
              <td><%= booking.formattedDate %></td>
              <td><%= booking.time %></td>
              <td class="<%= booking.status.toLowerCase() %>"><%= booking.status %></td>
              <td>
                <a href="/bookings/<%= booking._id %>/edit" class="action-btn edit">Sửa</a>
                <form action="/bookings/<%= booking._id%>/delete?_method=DELETE" method="POST" style="display:inline;">
                  <button type="submit" class="action-btn delete">Hủy</button>
                </form>
              </td>
            </tr>
          <% }); %>
        <% } %>
      </tbody>
    </table>
  </div>
</body>
</html>
